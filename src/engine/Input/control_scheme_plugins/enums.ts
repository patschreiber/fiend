/**
 * The built-in control scheme types. Allows a user to change the control scheme
 * without remapping all the keys individually.
 */
export enum DefaultControlSchemeTypes {
  DEFAULT,
  FPS,
  ONEHANDED_RIGHT,
  ONEHANDED_LEFT,
}
